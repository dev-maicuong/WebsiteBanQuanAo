@using WebsiteBanQuanAo.Models;
@model List<tbSanPham>

@foreach (var item in Model)
{
    float giasp = (float)item.GiaSanPham;
    <div class="recent-grids">
        <div class="recent-left">
            <a href="single.html"><img class="img-responsive " src="~/Content/Layout/images/@item.AnhMatTruoc" alt=""></a>
        </div>
        <div class="recent-right">
            <h6 class="best2"><a href="single.html">@item.TenSanPham </a></h6>
            <div class="block">
                @Html.Action("DanhGia2", "DanhGia2", new { masanpham = item.MaSanPham })
            </div>
            <span class=" price-in1"> @giasp.ToString("#,##0") VNĐ</span>
        </div>
        <div class="clearfix"> </div>
    </div>
}
